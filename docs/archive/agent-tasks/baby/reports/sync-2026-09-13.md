# 2026-09-13 pull / stash integration audit

## Outcome

- `git pull`: develop `f538c58` → `30af559` fast-forward; origin/develop synchronized.
- `git stash apply stash@{0}` restored work. Original stash remains as recovery copy, not popped/dropped.
- Text conflicts: `config/categories/baby.yaml`, `src/schemas.py`; resolved with HEAD (`30af559`) versions. Do not restore the old baby-only ConditionState/RecommendResult on top of them.
- Non-conflicting P0 code/0010/data/docs restored. P0 EvidenceRef/EvidenceRefs/ValidationIssue were salvaged into `src/reduction_contracts.py`, separate from current HTTP DTOs. P0 test imports/response fixture now use current RecommendResultOut. Latest HTTP schemas remain unchanged.
- `uv sync --locked` installed new psycopg_pool dependency. Initial collection failures from missing dependency and removed model imports were resolved.

## P0 judgment: partial; targeted continuation required, no full restart

The restored `reports/P0.md` already declared **partial**. It did not meet SR02/SR03/SR05. New upstream code increases the migration surface. Keep useful staging work and rebase the unfinished acceptance cases on the live application.

| Preserve | Rework / finish |
|---|---|
| 0010 staged columns/backfill and previous rehearsal evidence | Add next forward migration, do not edit previously applied checksum. Complete constraints, actual mapped conversions and old table removal. |
| BabyRequirement/BabyCandidate/CandidateCheck/BasketItem/BasketDecision | Validate units/IDs/types and wire actual producers; definitions alone are not integrated API. |
| JSON candidate refs and standalone JSON models | Existence/run scope/dedup checks; validation still writes old target/evidence tables. Current get_stored_result does not consume candidate evidence. |
| Alert route and UI removal | Schema notification/dataset/shared still exists; no complete reduction. |
| Upstream pool, session routes, slot_rules, frontend response DTOs | Extend, do not replace them with stubs or duplicate service/DTO stack. Preserve real PC 202/background execution. |

### New migration hazards reproduced

1. Empty install applies 0010 BEFORE new `db/seed.py` runs. Backfill sees no domain rows; later seed writes legacy domain_version only. Both baby/computer `current_version_no` remain NULL.
2. Latest PC executor uses `ensure_node`/`ensure_requirement` and old node FK; eight newly created requirements have NULL `slot_key`. `PlanRepo.load_full` restored from stash synthesizes nodes from those NULL keys. The current PC path still completes because it uses other legacy joins; this is not reduced-schema readiness.
3. Catalog seed writes `evidence.source` and `shared.unit`; ProductRepo and result query still depend on them/plan_node. Complete conversion must update all new upstream callers and bootstrap order.
4. Actual application table count is **60** (original58 + mapping table + item), not35. Five sampled removed-target tables all still exist. Schema/JSON constraints and referential validation are incomplete.
5. 0010's conflict guards mention reviewed mapping but do not consume mapping rows to resolve them. Seeded ambiguous category and multi-item allocation rehearsal remains required; adding a mapping row alone does not make current migration proceed.
6. Multiple guest sessions in same browser rotate cookie and lose previous guest-list access (404). P1 must fix. `age_stage` is display-only, age groups use representative months and prenatal uses0: never treat grouped ages as exact safety facts; P1/P3 require normalization/requestion.

## Authoritative current behavior

- POST session returns200 with truefit_guest; category/message/answer/GET work for baby with seeded DB. can_recommend=true means condition readiness only.
- Baby POST recommend remains501 in start_recommendation. The error string saying a separate pipeline exists does not mean HTTP baby recommendations work.
- PC POST recommend returns202; real BackgroundTasks executes under a separate connection. GET result returns done,8 items (this fixture total1493000). It still selects from CSV and uses demo checks; this is not baby safety validation or production-ready worker durability.
- Password login404, confirm/report/edit/review implementation still outstanding.
- Current public types: ConditionState, RecommendAcceptedOut, RecommendResultOut/ItemOut. progress=list, conditions_summary=str. Do not revert to stash's progress=dict/summary=dict/baby-only models. revision_id/lock_version/safety/evidence fields remain future compatible additions.
- Latest progress report says pgvector remains; older reduction proposal still describes separate vector DB as an assumption. Use current pgvector path for these tasks,35 is target, no vector migration without a new explicit change decision.

## Verification observed

| Check | Result |
|---|---|
| Base tests |42 passed,27 skipped,3 subtests passed|
| Clean disposable DB migration |0000–0010,11 migrations applied|
| Full SQL suite |68 passed,1 transport setup error,3 subtests passed; NOT a clean all-pass run|
| Isolated errored idempotent reingestion test |1 passed|
| RAG ingest/evaluate |5 chunks,21/21,local-test|
| Baby HTTP |create/category/message/answers/GET200, required fields persisted, recommendation501|
| Other guest access |404|
| Same guest creates second list then opens first |404, remaining bug|
| PC HTTP |202 then GET200 done with8 items|
| Password login |404|

PGlite socket server initially failed with concurrent pool sessions and duplicate prepared statement names. HTTP was rechecked with **test launcher only** max_size=1, prepare_threshold=None, no concurrent pytest. Do not change production pool defaults to hide this emulator limit. Test collection transport errors are recorded, not counted as passes. No current browser/real PostgreSQL multi-connection verification claimed.

## Reproduction (disposable DB only; run from repository root)

Use CONTRACTS DB startup commands, then:

```bash
uv sync --locked
export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:55432/postgres?sslmode=disable'
export RAG_TEST_DATABASE_URL="$DATABASE_URL"
uv run python db/migrate.py up
uv run python -m pytest -q
# Stop other clients before the following HTTP-only emulator launcher.
uv run python docs/agent-tasks/baby/reports/artifacts/pglite_http_server.py
# Second terminal:
uv run python docs/agent-tasks/baby/reports/artifacts/check_sync_http.py
```

Launcher executes existing seed functions and real uvicorn at18000 with one SQL connection; it neither mocks handlers nor returns fake results. Smoke uses local synthetic users/data, no outbound purchase. `check_sync_http.py` asserts current expected501/404 and is a dated audit script, NOT future desired-behavior acceptance. After P1/P5/P6 fixes, update/newly version the audit instead of preserving these failures.

Evidence: [structured audit](artifacts/sync-2026-09-13.json). Full RAG evaluator output from prior P0 remains historical; latest summary is in this audit.

## Next execution

1. Continue P0-SYNC delta in P0 work order, preserve current PC+guest baseline and finish real reduced schema.
2. P1 reuses working pool/handlers, fixes validation, guest identity reuse, normalized prenatal/exact age and draft locking; no ground-up rewrite.
3. P2 reuses shared ProductRepo/seed entrypoints but implements baby data/rules. P3/P4 remain baby work.
4. P5 extends existing start_recommendation/execute_recommendation/get_stored_result with baby; retain202, implement stale/terminal/recovery safeguards.
5. P6–P9 follow updated per-task deltas. Downstream completion may not assume P0 done solely because 0010 exists.
