# P9 운영 검증 보고

## 2026-09-14 v3 — develop `da79839` ACTIVE DB CONTRACT

```yaml
task_id: P9
contract_version: 3
target_db_commit: da798393850c03866374b05c01eed568fbc84f71
status: partial
implementation_commit_or_tree: "110f6ca + workspace changes"
service_db_applied: false
changed_files:
  - scripts/check_baby_readiness.py
  - tests/test_baby_readiness.py
  - docs/agent-tasks/baby/operating-decisions.md
  - src/services/recommendation_service.py
  - tests/test_baby_recommendation_http.py
  - generated/operations/readiness-p9-v3.json
reused_evidence:
  - report: reports/P3.md
    cases: [D3-01, VE01, VE06, VE07]
    rationale: "v3 external provider boundary, revoke and unavailable semantics; not a new backend run."
  - report: reports/P6.md
    cases: [D6 v3 schema round trip]
    rationale: "unchanged iat/auth path only; not DB/search certification for P9."
new_tests:
  - command: "DATABASE_URL=.../p9_v3_20260914 uv run python db/setup_all.py (twice)"
    result: "pass; first run applied exact 0000–0013 chain and seeded 51 PC catalog rows; second run applied no migration and did not duplicate domain versions."
  - command: "DATABASE_URL=.../p9_v3_20260914 uv run python scripts/seed_baby_catalog.py --corpus synthetic --dataset-version baby-demo-v1"
    result: "pass; 44 products/variants/offers and 44 observations created."
  - command: "DATABASE_URL=.../p9_v3_20260914 uv run python scripts/check_baby_readiness.py --corpus synthetic --provider unconfigured --output generated/operations/readiness-p9-v3.json"
    result: "pass (exit 0, status partial); DB/schema/domain/candidate/unit/API checks pass; provider and manual mapping explicitly blocked."
  - command: "DATABASE_URL=... RAG_TEST_DATABASE_URL=... uv run python -m pytest -q tests/test_baby_readiness.py tests/test_schema_reduction.py tests/test_baby_session_http.py tests/test_baby_recommendation_http.py tests/test_baby_verification.py"
    result: "52 passed, 0 failed, 2 framework deprecation warnings."
acceptance:
  D9_install_and_replay: pass
  D9_pc_regression: pass
  D9_baby_conditions_to_result: pass
  D9_search_unconfigured: pass
  D9_search_publish_search_revoke_real_backend: blocked
  D9_wrong_owner_and_concurrent_edit: pass
  D9_restart_after_worker_death: blocked
reproduction:
  - "Create a named empty disposable PostgreSQL DB; never use a service/production DB."
  - "DATABASE_URL=.../p9_v3_20260914 uv run python db/setup_all.py"
  - "DATABASE_URL=.../p9_v3_20260914 uv run python scripts/seed_baby_catalog.py --corpus synthetic --dataset-version baby-demo-v1"
  - "Run the readiness command and the 52-test command above."
artifacts:
  - generated/operations/readiness-p9-v3.json
  - docs/agent-tasks/baby/operating-decisions.md
remaining:
  - "No selected third-party external search backend/credentials or authorized real manuals; D3-02 and D9 publish/search/revoke against that backend remain blocked."
  - "No durable worker or startup reconciliation exists for a run left running by process death; restart recovery is unverified."
  - "O2 requires authorized documents, criteria, model/region/profile; O3/O4/O5 are not selected."
```

### 실제 관측과 D9 사례

| 사례 | 실제 결과 |
|---|---|
| 빈 DB 설치·재실행 | develop의 전체 파일명 체인 `0000_prereq`부터 `0013_result_item_interaction`까지 적용됐다. 두 번째 `setup_all.py`는 적용할 migration이 없었고 domain version과 PC 시드를 중복 생성하지 않았다. |
| v3 목표 스키마 | readiness가 `domain_version`, `plan_node`, `purchase_line`, material/evidence/review/price_watch 존재와 `rag`, `dataset`, `planning.item` 부재를 확인했다. |
| 유아 시드·조건→결과 | synthetic 시드의 8개 필요영역(의류의 명시적 data gap 제외)이 후보를 보유했고 가격/단위 오류는 0건이었다. FastAPI 실제 DB 경로에서 조건→202→terminal result, 편집·동시성·권한 사례를 포함한 P5 테스트가 통과했다. 결과 재조립의 잔존 `fulfilled_by_item_id`/`planning.item` 참조를 발견해 v3 `owned` 조건 출처와 `recommendation_candidate` 상태로 교체했다. |
| PC 회귀 | 같은 전용 DB에서 `setup_all.py`의 PC 51행 시드와 schema/session/recommendation 회귀 묶음이 통과했다. 이것은 가격·추천 품질이나 외부 검색의 새 증거는 아니다. |
| 미설정 검색 | `BABY_SEARCH_PROVIDER`가 없으면 readiness는 `search_provider_unconfigured`을 blocked로, 앱 계약은 unknown/자동선택 불가로 기록한다. import나 PC 경로가 차단되지 않으며 PostgreSQL rag fallback은 없다. |
| 검색 철회 | P3의 local-file provider/revoke와 DB evidence 재검사 테스트를 52건 묶음에 포함했다. 이는 로컬 개발 provider 증거이며 실제 선택된 외부 backend의 health/publish/search/revoke 증거가 아니다. |
| 재시작 | HTTP TestClient의 terminal 결과 저장은 확인했지만, 프로세스가 BackgroundTasks 도중 죽은 `running` 행의 복구는 구현·검증되지 않았다. |

### O1–O5 상태

O1은 **partial**이다: 읽기 전용 readiness 도구와 일회용 DB 리허설은 완료했지만, 운영 검색/worker 복구는 미충족이다. O2는 **blocked**, O3/O4/O5는 **not_selected**이며, 상세 활성화 입력은 [`operating-decisions.md`](../operating-decisions.md)에 기록했다. 따라서 이 보고서는 서비스 운영 준비 완료를 선언하지 않는다.

`scripts/check_baby_readiness.py`는 migration/seed/추천/검색을 수행하지 않는다. JSON에는 DSN을 넣지 않으며, DB·migration·스키마·domain hash·필요영역 후보·가격/단위·매뉴얼 mapping·API/run 상태를 분리해 기록한다. required failure에는 nonzero로 끝나고, provider 미선택처럼 명시적 외부 gate는 `partial`로 드러낸다.
