---
task_id: P1
status: partial
commit_or_tree: "30af559 + this task's uncommitted workspace changes"
dependency_revisions: {P0: "30af559 + 0010/0011/0012/0013 (P0 v4, reused as-is)"}
implemented_contract_version: 2
changed_files:
  - pyproject.toml
  - uv.lock
  - src/schemas.py
  - src/routers/session.py
  - src/services/session_service.py
  - src/repo/plan_repo.py
  - src/repo/user_repo.py
  - src/engine/slot_rules.py
  - config/categories/baby.yaml
  - tests/test_baby_session_http.py (new)
tests:
  - {command: "uv run python -m pytest -q tests/test_baby_session_http.py", result: "pass (real PostgreSQL + real FastAPI TestClient, no mocks)", passed: 11, failed: 0, skipped: 0}
  - {command: "uv run python -m pytest -q (full suite, fresh disposable DB, SQL tests enabled)", result: "pass", passed: 111, failed: 0, skipped: 0}
  - {command: "real HTTP walk via uvicorn (see reproduction) — guest cookie reuse across two lists", result: "POST /session (list A) -> POST /session again with same cookie (list B, same guest cookie value, not rotated) -> GET list A = 200, GET list B = 200", passed: 1, failed: 0, skipped: 0}
  - {command: "real HTTP walk — cross-guest isolation and malformed UUID", result: "guest B GET on guest A's list = 404; GET /session/not-a-uuid = 422", passed: 1, failed: 0, skipped: 0}
  - {command: "real HTTP walk — message extraction + budget clear/history", result: "POST message '8개월, 예산 30만원' -> age_stage.months=8 status=confirmed, budget_max=300000; PATCH slot budget_max=null -> status=missing, old plan_condition row status='superseded' in DB", passed: 1, failed: 0, skipped: 0}
  - {command: "real HTTP walk — computer (PC) category regression", result: "POST category=computer -> 200; POST message -> 200; POST recommend without all required fields -> 422 conditions_incomplete (expected, unrelated to this change; PC condition path unaffected)", passed: 1, failed: 0, skipped: 0}
reproduction:
  - "docker exec <pg-container> createdb -U truefit p1_check"
  - "export DATABASE_URL=postgresql://truefit:truefit@127.0.0.1:5432/p1_check?sslmode=disable"
  - "export RAG_TEST_DATABASE_URL=$DATABASE_URL"
  - "uv run python db/setup_all.py"
  - "uv run python -m pytest -q                       # 111 passed, 3 subtests, 0 skipped"
  - "uv run python -m pytest -q tests/test_baby_session_http.py   # 11 passed"
  - "uv run uvicorn src.api:app --host 127.0.0.1 --port 8000 &"
  - "curl -c /tmp/truefit-guest.txt -X POST http://127.0.0.1:8000/session   # -> {list_id}, Set-Cookie: truefit_guest"
  - "curl -b /tmp/truefit-guest.txt -c /tmp/truefit-guest.txt -X POST http://127.0.0.1:8000/session  # second list, SAME cookie value (guest identity reused)"
  - "curl -b /tmp/truefit-guest.txt http://127.0.0.1:8000/session/<list_id_A>   # still 200, not 404"
artifacts: []
remaining:
  - "Optional safety fields (weight_kg, independent_sitting) are asked as follow-up questions only once a relevant need (외출/안전·건강) is confirmed, and are stored/validated, but no P3 rule engine consumes them yet — that is explicitly P3/P5 scope, not a P1 gap. can_recommend never depends on them (by design, per CONTRACTS)."
  - "Baby POST /recommend remains 501 (start_recommendation has no baby rule/catalog wiring) — unchanged from the P0 baseline and explicitly out of P1 scope per CONTRACTS (P5 supplies it)."
  - "load_owned_draft/normalize_baby_conditions are implemented and exercised indirectly (all session routes use load_owned_draft; normalize_baby_conditions has no caller yet since P5/P7 don't exist) — P5/P7 must adopt them rather than re-implementing condition access/normalization."
  - "Cookie Secure/HTTPS-mode flag was not touched in this task (pre-existing behavior); CONTRACTS' 'Secure on HTTPS, explicit local HTTP mode' is not separately re-verified here."
  - "Concurrency proof (SS05) used 5 threads issuing PATCH against the same revision/key over separate connections against the real pool; it demonstrates no duplicate active row and a stable lock_version increment, not a full production load test."
---

# P1 execution report — DB 접속·게스트 세션·조건 수집

## 실행 요약

이 과업은 `docs/agent-tasks/baby/reports/sync-2026-09-13.md` 가 명시한 실제 결함들을 대상으로
했다. 새 파이프라인이나 목(mock) DB를 만들지 않고, 기존 pool/lifespan/`session_service`/
`PlanRepo`/`routers/session.py` 를 그대로 이어받아 수정했다.

### 고쳐진 실측 결함 (baseline delta 대응)

1. **게스트 정체성 재사용** (`create_session`, `src/services/session_service.py`).
   기존 코드는 `/session` 을 호출할 때마다 무조건 새 토큰을 발급해 쿠키가 회전했고, 그 결과
   이전 목록이 404 가 됐다(sync 감사에 재현됨). 이제 유효한 기존 게스트 쿠키가 오면
   `ConversationRepo.guest_identity_known()` 으로 실제로 존재하는 정체성인지 DB 조회로 확인한
   뒤 재사용하고, 미지/위조 쿠키만 새 토큰으로 교체한다. 위의 실 HTTP 재현에서 같은 쿠키로
   목록 A/B 를 만든 뒤 둘 다 200 임을 확인했다.
2. **age_stage 대표값/정확값 구분** (`_unwrap_age`, `config/categories/baby.yaml`). 개월수는
   `{"value":int,"exact":bool}` 로 저장해, 칩(`q_age`, 대표값 1/5/9/18/30)으로 답한 경우
   `status=assumed`, "8개월" 같은 자유 텍스트 정확값은 `status=confirmed` 로 구분한다.
   prenatal 의 `due_date` 는 별도 질문(`q_due_date`)과 별도 필드로 분리해 이전에 있던
   "출산 예정=0, 신생아=0" 충돌을 없앴다.
3. **필수/none/타입 검증** (`_validate_baby_value`). baby 카테고리 전용으로 필드 화이트리스트,
   타입(정수/금액/실수/불/날짜/목록), needs 허용값, none 상호배타, mode·필드 일관성(예:
   prenatal 에서 age_months 거부)을 검증한다. computer 카테고리는 이 경로를 타지 않아
   기존 PC 조건 처리(실 HTTP 재현으로 재확인)는 그대로 보존된다.
4. **missing vs 명시적 "없음" 구분** (`compute_missing`, `_build_fields`). 조건 행이 존재하는지
   (활성 `plan_condition` 행 유무)로 판단하도록 바꿔, `health_skin=[]`("특이사항 없음" 응답)이
   더 이상 missing 으로 오분류되지 않는다. 실 HTTP/pytest 로 확인.
5. **"없어요" 과잉 추출 버그** (`src/engine/slot_rules.py::extract_baby`). "보유 물품이 없어요"
   같은 문장이 건강 상태를 "특이사항 없음"으로 잘못 단정하던 버그를 맥락 앵커(피부/건강/특이사항/
   알러지 키워드 인접)로 분리했고, need 키워드도 부분 문자열이 아니라 CONTRACTS 의 정식
   9개 라벨로 매핑한다.
6. **카테고리/모드 전환 시 조건 정리** (`choose_category`). 카테고리 자체가 바뀌면 category/mode
   외 모든 조건을 지우고, baby 안에서 born↔prenatal 전환 시 서로 호환 안 되는 필드
   (age_months/weight_kg/independent_sitting ↔ due_date)만 지운다(SS06).
7. **PATCH null = 진짜 clear, POST reset = 조건+대화 비우기** (`PlanRepo.clear_condition`,
   `ConversationRepo.delete_messages`, `reset_conditions`). 이전에는 clear 가 대체 행을 만들어
   값이 `None` 인 새 활성 행이 남았다; 이제는 활성 행을 superseded 로만 만들고 새로 삽입하지
   않아 missing 으로 정확히 되돌아간다. `/reset` 은 카테고리/모드만 남기고 조건과 대화 이력을
   실제로 비운다.
8. **동시 쓰기 경합** (`PlanRepo._lock_revision`). 최초 값이 아직 없는 키에 대해 두 요청이
   동시에 들어오면 `SELECT ... FOR UPDATE` 로 잠글 활성 행이 없어 둘 다 "old 없음"으로 판단해
   INSERT 하다가 `plan_condition_active_key` unique 제약을 위반하는 실제 경합을 재현으로
   확인했다. 조건 행이 아니라 존재가 보장된 `plan_revision` 행을 먼저 잠가 같은 리비전에 대한
   조건 쓰기 전체를 직렬화하도록 고쳤다 — 5개 스레드로 동시 PATCH 를 쏘는 테스트
   (`test_ss05_concurrent_slot_writes_leave_one_active_row`)로 활성 행이 항상 정확히 1개임을
   실측했다.
9. **`load_owned_draft`/`normalize_baby_conditions` 공개** — P5/P7 이 재사용할 소유권 검증과
   조건 정규화 진입점을 `src/services/session_service.py` 에 노출했다. 기존 `/recommend`,
   `/result` 라우트도 이 함수로 통일했다(이전에는 `PlanRepo(conn)` 을 라우터에서 직접 만들어
   내부 헬퍼에 넘기는 임시 방식이었다).
10. **`ConditionState` 에 `mode`/`revision_id`/`lock_version` 노출** (`src/schemas.py`) —
    CONTRACTS 가 명시한 공개 필드를 실제로 반환하도록 추가했다(이전에는 내부적으로만 존재).

### 검증 방식 — 실측, mock 없음

- `tests/test_baby_session_http.py` (신규, 11개) 는 실 PostgreSQL 16(로컬 docker, pgvector)과
  실 FastAPI `TestClient` 두 개(게스트 A/B)로 SS01~SS06 각각을 실행하고, 별도 psycopg 연결로
  `planning.plan_condition`/`planning.plan_revision`/`identity.message` 실제 행을 대조한다.
  `get_conn()`/커넥션 풀은 대체하지 않았다 — 각 HTTP 요청이 실제 트랜잭션 1개를 커밋/롤백한다.
- 전체 스위트(P0 것 포함) 는 매번 새로 만든 일회용 DB에 `db/setup_all.py` 를 돌려
  0000~0013 마이그레이션과 시드를 적용한 뒤 실행했다(재현 명령대로 3회 반복 확인, 매번
  111 passed / 0 skipped).
- 실 `uvicorn` 프로세스로 curl 워크를 두 차례 수행했다(게스트 재사용/타인 접근 차단/조건
  추출/기록 보존, 그리고 computer 카테고리 회귀 확인) — 위 `reproduction`/`tests` 블록에
  실제 커맨드와 관측값을 기록했다.
- `httpx` 를 새 의존성으로 추가했고(`pyproject.toml`/`uv.lock`, `uv lock` 로 재해석), FastAPI
  `TestClient` 가 이를 통해 동작함을 확인했다.

## SS01–SS06 대조표 (실측)

| 케이스 | 결과 |
|---|---|
| SS01 쿠키 없음→생성→Set-Cookie, category=null→can_recommend=false, 재요청 시 동일 상태 | pass (실 HTTP + pytest) |
| SS01 재사용 게스트가 여러 목록 생성, 첫 목록 계속 접근 가능 | pass — 이전엔 404였던 정확한 재현 시나리오가 이제 200 (실 HTTP + pytest) |
| SS02 "8개월, 예산 30만원" → months=8(confirmed)/budget=300000, 칩으로 나머지 채우면 can_recommend=true; 예산 clear→false, 이전 조건은 superseded 로 보존 | pass |
| SS02 "특이사항 없음" 은 missing 이 아니라 confirmed(값 `[]`) | pass |
| SS03 몰라요/음수 예산/소수 개월/미지 필드/none+other/잘못된 날짜 → missing 또는 422, 잘못된 조건 행 미삽입 | pass |
| SS04 다른 게스트 GET/PATCH/reset 차단, 위조/누락 쿠키 차단, 잘못된 UUID 는 통제된 검증 오류(422) | pass |
| SS05 거부된 입력이 lock_version/메시지를 남기지 않음, 5-스레드 동시 PATCH 에도 활성 조건 행 정확히 1개 | pass |
| SS06 prenatal↔born 독립 왕복, 모드 전환 시 비호환 필드 삭제(이전 값 자동 복원 없음) | pass |

## 미충족/범위 밖 (정직하게 partial 로 유지하는 이유)

- baby `POST /recommend` 는 여전히 501 이다 — CONTRACTS 가 P5 소관으로 명시한 경계이며, 이
  과업은 조건 수집·검증·소유권·동시성만 다룬다. 이를 "완료"로 표기하지 않는다.
  `normalize_baby_conditions`/`load_owned_draft` 는 실제로 구현·연결(라우터가 씀)했지만
  P5/P7 가 아직 없어 `normalize_baby_conditions` 자체의 실제 호출자는 이번 과업 안에는 없다 —
  함수 존재와 동작(단위 검증)은 확인했으나 "P5가 이걸 실제로 썼다"는 사실은 아니다.
- weight_kg/independent_sitting 질문 게이팅(관련 need 확인 후에만 질문)은 구현·검증했지만,
  이 값들을 소비하는 안전 규칙 자체는 P3 범위라 이번 과업에는 없다.
- 동시성 테스트는 실 커넥션 풀에 대한 5-스레드 재현이며, 프로덕션 부하 테스트는 아니다.
- Secure 쿠키 플래그/HTTPS 명시 모드는 이번 과업에서 다시 손대지 않았다(기존 동작 유지).

이 두 가지(baby 추천 501, 안전 규칙 미구현)는 작업지시서 자신이 P5/P3 소관으로 명시한 경계이므로
P1 결함이 아니다. 그럼에도 상태를 `complete` 로 올리지 않는 이유는, 이 보고서가 실측한
SS01–SS06 은 전부 통과했지만 CONTRACTS.md 소유자가 이 보고서를 검토해 상태를 올리는 것이
그 문서 자신의 종료 조건이기 때문이다(P0 보고서들과 동일한 원칙).

## 재검증 — 병행 작업(P2/P8 등) 병합 이후 (같은 날, HEAD `d96ccd2`)

이 과업 실행 이후 저장소에 다른 태스크(P2 유아 카탈로그/요구사항, P8 리뷰 등)의 작업이
같은 워킹트리에 추가로 쌓였다(`config/baby_requirement_rules.yaml`, `scripts/seed_baby_catalog.py`,
`0010_review_summary_relation_axis.sql` 등). 위 구현이 그 위에서도 실제로 깨지지 않았는지
새 일회용 DB로 다시 실측했다 — 코드는 변경하지 않고 재현만 반복했다.

```yaml
reverify:
  head: d96ccd2e18a83e8e0115a1accc936a9239e5db73
  db: "일회용 p1_reverify (새로 생성 → 검증 후 삭제)"
  tests:
    - {command: "uv run python db/setup_all.py", result: "pass; 0000-0009, 0010_review_summary_relation_axis, 0010_schema_reduction_v1, 0011-0013 적용 + 시드", passed: 1, failed: 0, skipped: 0}
    - {command: "uv run python -m pytest -q (전체 스위트)", result: "pass — 이번 과업 실행 시점(111 passed)보다 늘어난 전체 스위트도 회귀 없음", passed: 184, failed: 0, skipped: 2}
    - {command: "uv run python -m pytest -q tests/test_baby_session_http.py", result: "pass, 변경 없음", passed: 11, failed: 0, skipped: 0}
    - {command: "실 uvicorn HTTP 워크 (게스트 재사용 + 조건 추출 + 타인 차단)", result: "POST /session(A) -> POST /session(B, 같은 쿠키) -> GET A=200, GET B=200; message '8개월, 예산 30만원' -> age_stage.months=8, budget_max=300000; guest B의 A 접근=404", passed: 1, failed: 0, skipped: 0}
  note: "2 skipped 는 pandas 미설치로 인한 tests/test_rank_review_axis.py, tests/test_relation_axis.py — P8 소관 리뷰 분석 옵션 의존성 그룹, 이 과업과 무관."
```

P2가 건드린 파일(`config/baby_requirement_rules.yaml`, `src/repo/product_repo.py`,
`src/engine/stage2_requirement.py`, `src/engine/stage3_0_candidates.py`)은 이 과업의 EDIT
SURFACE(`config/categories/baby.yaml`, `src/services/session_service.py`,
`src/repo/plan_repo.py` 등)와 겹치지 않아 병합 충돌은 없었다. `db/migrations/` 에 번호가
겹치는 `0010_review_summary_relation_axis.sql`/`0010_schema_reduction_v1.sql` 두 파일이 공존하는
것은 확인됐으나 — `db/migrate.py` 가 파일명 전체로 식별해 순서대로 둘 다 적용하므로 실행에는
문제가 없었다. 이 번호 중복은 P1 소관 파일이 아니라 언급만 남긴다(고치지 않음).

상태는 이번 재검증에서도 `partial` 로 유지한다 — 결론은 위 실행 요약과 동일(baby 추천 501/안전
규칙은 P5/P3 소관)하고, 이번 재검증은 그 결론이 이후 병합에도 깨지지 않았다는 추가 증거일 뿐이다.

## v3 — develop DB 정렬 (`da79839`) 아래 domain_version_id 재검증 (2026-09-14)

[ACTIVE DB CONTRACT](../P1_sessions_conditions.md)·[develop 전환 계약](../DEVELOP_DB_TRANSITION.md)·
[목표 스키마](../schema-v1.md)를 읽고 P0의 실제 v3 산출물([P0.md](P0.md)의 최신 절,
[P0 develop 전환 검토](P0-develop-review-2026-09-14.md))을 대조했다. 이 세션을 시작할 때 이미
`src/repo/plan_repo.py`(`bind_domain_version`/`published_domain_version`)와
`src/services/session_service.py` 가 domain_version_id 모델로 전환되어 있었다 — 다른 세션이
이번 작업 전에 같은 방향으로 구현했고, 여기서는 그 결과를 실제로 재현·보완했다. 완전 축소
SQL(`0010_schema_reduction_v1.sql` 등, rag 계열 5개)이나 PostgreSQL rag 스키마를 복원하지
않았다 — 현재 `db/migrations/`는 develop 원본 체인(`0000`–`0009`, `0010_review_summary_relation_axis`,
`0011_drop_rag_schema`, `0012_schema_reduction_safe_subset`, `0013_result_item_interaction`)만
갖고 있고, 이번 작업에서 이 파일 목록을 바꾸지 않았다.

### 실제로 고친 결함 — PC 사양 업로드 회귀 (D1 "PC 사양 업로드... 보존" 미충족이었음)

병합 과정에서 develop의 `POST /session/{list_id}/spec-file` 라우트와 `schemas.SpecFileIn`이
빠졌고, `session_service.attach_spec_file`은 정의되지 않은 `FileTooLarge`를 참조해 실제
호출하면 `NameError`로 500이 났을 것이다(라우트가 없어 이전에는 호출 경로 자체가 없었다).
`ConditionState.accepts_spec_file`도 baby 이식 당시 값이 항상 `False`로 고정된 채 남아 있어
PC upgrade 모드에서도 업로드 허용 여부를 알려주지 못했다. 세 가지를 모두 고쳤다:

1. `src/errors.py` — develop과 동일한 `FileTooLarge`(413, `file_too_large`) 복원.
2. `src/services/session_service.py` — `attach_spec_file`가 `FileTooLarge`를 import해서 쓰도록
   수정, 소유권 검사를 중복 정의된 구식 `_owned(repo,...)` 대신 이 파일의 정식
   `load_owned_draft(conn,...)`로 통일(중복 함수 삭제), `accepts_spec_file`을
   `category == "computer" and mode == "upgrade"`로 실제 계산(develop 원본 로직과 동일).
3. `src/schemas.py`, `src/routers/session.py` — `SpecFileIn`과 `POST /{list_id}/spec-file`
   라우트를 develop 원본과 동일한 자리에 복원.

### D1 수용 사례 — 실측 (신규 `tests/test_p1_d1_develop_db.py`, 6개)

`tests/test_baby_session_http.py`(SS01–SS06, 11개)는 이미 domain_version_id 모델 위에서
전부 통과해 재사용했다(코드 변경 없이 재실행만) — D1의 "같은 브라우저 두 목록 접근", "게스트
교차 접근 거부", "조건 수정 후 재조회" 상당 부분을 그 파일이 이미 커버한다. 신규 파일은 D1이
명시했지만 그 파일이 직접 재현하지 않는 나머지를 추가했다:

| D1 항목 | 방법 | 결과 |
|---|---|---|
| baby↔computer 전환 시 서로 다른 domain_version 행 | `planning.plan_revision.domain_version_id`를 `config.domain_version`/`config.domain`과 조인해 실제 코드·상태·해시 대조 | pass — baby 는 `status='draft'`(스텁, develop 원본 의도)인데도 실제 `content_hash`를 가진 그 카테고리 전용 domain_version에 고정됨을 확인. computer 전환 시 `domain_version_id`가 실제로 바뀜(다른 UUID) |
| 같은 게스트, baby 목록 1개 + computer 목록 1개 동시 접근 | 두 목록 생성 후 각각 GET, 그리고 각 domain_version 행 대조 | pass |
| exact=false 보존 | `/answer`로 `q_age`(대표값) 응답 → `status=assumed`/`exact=false` → GET 재조회에도 동일 | pass |
| 조건 수정 후 재조회 | PATCH slot 두 번 연속, 매번 GET으로 최신값 확인 | pass |
| 게스트 교차 접근 거부 | 다른 TestClient(다른 쿠키)로 GET/PATCH → 404 | pass |
| PC 사양 업로드 보존 | `category=computer, mode=upgrade` → `accepts_spec_file=true` 확인 → 실제 파일 업로드 → `planning.plan_condition`에 `current_specs` 행 실측(파싱된 CPU/GPU 값 확인) → 과대 용량 413, 미지원 확장자 422 | pass |
| baby accepts_spec_file=false 보존 | baby, computer/build(비업그레이드) 각각 GET → 둘 다 false | pass |

```yaml
contract_version: 3
target_db_commit: da798393850c03866374b05c01eed568fbc84f71
status: partial
implementation_commit_or_tree: "d7e6349 + this task's uncommitted workspace changes"
changed_files:
  - src/errors.py
  - src/schemas.py
  - src/routers/session.py
  - src/services/session_service.py
  - tests/test_p1_d1_develop_db.py (new)
reused_evidence:
  - {report: "P1.md 본문(첫 실행, 2026-09-13)", cases: ["SS01", "SS02", "SS03", "SS04", "SS05", "SS06"], rationale: "unchanged_path — tests/test_baby_session_http.py 는 domain_version_id 모델로 이미 전환된 코드 위에서 코드 변경 없이 재실행만 했고, 11개 전부 이번에도 동일하게 통과했다(재현 기록 위 '재검증' 절 참조). DB 어댑터가 바뀌었어도 세션/조건 HTTP 계약 자체는 이 사례들에서 동일하게 성립한다."}
new_tests:
  - {command: "uv run python -m pytest -q tests/test_p1_d1_develop_db.py -v", result: "pass, 6/6", cases: ["baby<->computer domain_version 분리", "동일 게스트 두 카테고리 목록 동시 접근", "exact=false 보존", "조건 수정 후 재조회", "게스트 교차 접근 거부", "PC 사양 업로드 + baby/build accepts_spec_file=false"]}
  - {command: "uv run python -m pytest -q tests/test_baby_session_http.py", result: "pass, 11/11 (재사용, 코드 변경 없음)", cases: ["SS01-SS06"]}
  - {command: "uv run python -m pytest -q (전체 스위트, 완전히 새 일회용 DB)", result: "219 passed, 12 failed, 30 skipped — 12건은 이 과업 변경 전과 동일한 사전 결함(아래 remaining), 30건은 pandas 미설치 옵션 그룹, 내 변경으로 인한 신규 실패 없음", cases: []}
acceptance: {D1: pass}
reproduction:
  - "docker exec <pg-container> createdb -U truefit p1_d1_check"
  - "export DATABASE_URL=postgresql://truefit:truefit@127.0.0.1:5432/p1_d1_check?sslmode=disable"
  - "export RAG_TEST_DATABASE_URL=$DATABASE_URL"
  - "uv run python db/setup_all.py   # develop 원본 체인 0000-0013 적용 + 시드(baby domain은 status=draft로, computer는 active로 — develop 원본 의도 그대로)"
  - "uv run python -m pytest -q tests/test_p1_d1_develop_db.py tests/test_baby_session_http.py -v   # 17 passed"
  - "uv run uvicorn src.api:app --host 127.0.0.1 --port 8000 &"
  - "curl -c jar -X POST http://127.0.0.1:8000/session; curl -b jar -X POST http://127.0.0.1:8000/session/<id>/category -d '{\"category\":\"baby\",\"mode\":\"born\"}'; # domain_version_id 는 baby 전용 draft 도메인의 실제 버전에 고정된다"
  - "curl -b jar -X POST http://127.0.0.1:8000/session/<id2>/category -d '{\"category\":\"computer\",\"mode\":\"upgrade\"}'; curl -b jar -X POST http://127.0.0.1:8000/session/<id2>/spec-file -d '{\"file_name\":\"pc.txt\",\"content\":\"CPU: i5-13600K\"}'  # 200, current_specs 저장"
remaining:
  - "P0 자체가 아직 R1-R4(교차 revision candidate 저장 미검증, 근거 JSON 구조/스코프 미검증, develop 가격 알림 라우트 누락, 확정 시 편집된 qty/timing 무시)로 partial이다 — 이 파일들은 P1 EDIT SURFACE 밖이라 여기서 고치지 않았다. P0 develop-review 문서가 '통합 완료 승인 보류'를 명시하므로, 이 P1 v3도 D1 자체는 pass지만 상위 P0 게이트가 열릴 때까지 전체 서비스 관점의 complete는 아니다."
  - "이번 세션 시작 시점에 이미 존재하던 12건의 실패(tests/test_baby_recommendation_http.py, tests/test_baby_requirements.py, tests/test_p1234_review_fixes.py)는 P2/P5 영역(src/engine/stage2_requirement.py 가 develop의 PlanRepo.ensure_requirement(revision_id, node_id, match_spec) 시그니처가 아니라 구 rag 축소 모델의 slot_key/group_key 인자를 그대로 호출)이다 — P1 EDIT SURFACE(session_service.py/plan_repo.py 의 domain_version 결합)와 무관해 손대지 않았다. DEVELOP_DB_TRANSITION.md의 '다음 단계' 절도 이 수정을 P2 몫으로 명시한다."
  - "baby domain 이 develop 원본대로 status='draft'로 유지된다 — 세션/카테고리/조건 저장은 이 상태에서도 동작함을 실측했지만(위 표), 실제 유아 추천 실행(POST recommend)은 여전히 P5 몫이며 이 v3에서 그 경로를 새로 연 것은 아니다."
service_db_applied: false
```

### 정직성 노트

이번 v3 는 `docs/agent-tasks/baby/P0.md` 최신 절의 develop 정렬 절과 `da79839` 원본을 직접
대조해 실제로 빠진 것(라우트·스키마·예외 클래스)만 골라 고쳤다 — 이미 동작하던
`bind_domain_version`/`published_domain_version`/조건 검증/게스트 재사용 로직은 그대로 재사용했고
다시 작성하지 않았다. `tests/test_baby_session_http.py`의 11개 통과는 "코드 변경 없이 재실행한
결과"이지 이번 세션이 새로 만든 증거가 아니므로 `reused_evidence`에 명시했다. `service_db_applied:
false`는 이번 검증이 전용 일회용 DB(위 재현 절차로 매번 새로 만들고 검증 후 삭제)에서만 이뤄졌고
실제 서비스 DB에는 아무 것도 적용하지 않았다는 뜻이다.

## 2026-09-14 develop-review fix — R1/R2/R3

[P1 develop-review](P1-develop-review-2026-09-14.md)의 R1/R2/R3을 해결했다.

```yaml
contract_version: 3
status: partial
changed_files:
  - src/repo/plan_repo.py
  - src/services/session_service.py
  - db/seed.py
  - tests/test_p1_d1_develop_db.py
new_tests:
  - command: "DATABASE_URL=<disposable> uv run python -m pytest -q tests/test_p1_d1_develop_db.py tests/test_baby_session_http.py"
    result: "19 passed"
    cases: [test_d1_published_domain_version_excludes_non_active_domains, test_d1_repeat_category_choice_keeps_pinned_domain_version, test_d1_pc_spec_file_upload_and_baby_accepts_spec_file_false (extended)]
acceptance: {R1: fixed, R2: fixed, R3: fixed}
remaining: []
service_db_applied: false
```

- **R1 (활성 도메인 선택 미충족)**: `db/seed.py`가 이제 `baby`도 `computer`처럼 `status='active'`로 심는다 — baby는 더 이상 스텁이 아니라 P1~P9로 실제 구현된 카테고리이므로, `PlanRepo.published_domain_version`이 `d.status='active'` 조건을 실제로 걸어도(이번에 추가) 세션이 baby를 여전히 고를 수 있다. `bind_domain_version`은 리비전이 이미 그 카테고리 코드로 묶여 있으면 저장된 domain_version_id를 그대로 유지하고(재선택마다 최신 version_no로 다시 묶지 않음), 다른 카테고리로 바뀔 때만 `published_domain_version`을 다시 조회한다. 기존 `test_d1_category_switch_pins_distinct_domain_version_rows`의 "baby.status='draft'가 성공 조건" 서술을 "active"로 정정하고, "같은 카테고리 재선택 시 최신 버전으로 옮겨가지 않음"과 "draft/disabled 도메인은 선택 불가"를 검증하는 테스트 2건을 추가했다.
- **R2 (owned_items 수량 정규화 누락)**: `_validate_baby_value`가 `owned_items` 필드는 `dict.fromkeys` dedup을 건너뛰도록 분리했다 — 반복된 라벨이 수량 신호이므로(P2가 이를 세도록 함께 고쳤다) 여기서 뭉개면 안 된다. `needs`/`health_skin` 등 다른 list 필드의 dedup은 그대로 유지했다.
- **R3 (업로드 허용 플래그와 서버 동작 불일치)**: `attach_spec_file`이 이제 `category=='computer' and mode=='upgrade'`가 아니면 명시적으로 거부한다(422, `spec_file_not_accepted`) — 과거엔 소유권/확장자/용량만 검사해 baby나 computer/build로 직접 POST하면 저장이 됐다. 기존 테스트의 "baby 업로드도 거절해야 한다"는 주석 옆에 실제 POST 후 422와 "조건 행이 안 남았는지" 검증을 추가했고, computer/build 케이스도 추가했다.
